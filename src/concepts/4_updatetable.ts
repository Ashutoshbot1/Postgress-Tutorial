/*
 * 1. Update data in table
 * UPDATE table_name SET column1 = value1, column2 = value2 WHERE condition;
 *
 * Example:
 * UPDATE users SET name = 'John Doe', email = [EMAIL_ADDRESS]' WHERE id = 1;
 */

/*
 * 2. Update multiple columns in table
 * UPDATE table_name SET column1 = value1, column2 = value2 WHERE condition;
 *
 * Example:
 * UPDATE users SET name = 'John Doe', email = [EMAIL_ADDRESS]' WHERE id = 1;
 */

/*
 * 3. Update data in table with multiple conditions
 * UPDATE table_name SET column1 = value1, column2 = value2 WHERE condition1 AND condition2;
 *
 * Example:
 * UPDATE users SET name = 'John Doe', email = [EMAIL_ADDRESS]' WHERE id = 1 AND name = 'John Doe';
 */

/*
 * 4. Update data in table with OR condition
 * UPDATE table_name SET column1 = value1, column2 = value2 WHERE condition1 OR condition2;
 *
 * Example:
 * UPDATE users SET name = 'John Doe', email = [EMAIL_ADDRESS]' WHERE id = 1 OR name = 'John Doe';
 */

/*
 * 5. Update data in table with NOT condition
 * UPDATE table_name SET column1 = value1, column2 = value2 WHERE NOT condition;
 *
 * Example:
 * UPDATE users SET name = 'John Doe', email = [EMAIL_ADDRESS]' WHERE NOT id = 1;
 */

/*
 * 6. Update data in table with IN condition
 * UPDATE table_name SET column1 = value1, column2 = value2 WHERE column IN (value1, value2, value3);
 *
 * Example:
 * UPDATE users SET name = 'John Doe', email = [EMAIL_ADDRESS]' WHERE id IN (1, 2, 3);
 */
