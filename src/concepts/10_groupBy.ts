/*
 * 1. GROUP BY
 * GROUP BY groups rows that have the same values in specified columns into summary rows.
 * GROUP BY can be used with aggregate functions to perform calculations on each group.
 */
