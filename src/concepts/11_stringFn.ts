/*
 * 1. CONCAT
 * CONCAT(string1, string2, ... stringN) returns a string that is the result of concatenating the input strings.
 */

/*
 * 2. AS
 * AS is used to rename a column or table in a query.
 * e.g SELECT name AS full_name FROM users;
 */
